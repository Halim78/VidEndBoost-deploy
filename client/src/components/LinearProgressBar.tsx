import Box from "@mui/material/Box";
import LinearProgress from "@mui/material/LinearProgress";

const LinearProgressBar = () => {
  return (
    <Box sx={{ width: "100%" }} className="pt-2 mx-auto">
      <LinearProgress />
    </Box>
  );
};

export default LinearProgressBar;
